# Postgres connection string (Supabase, Neon, Railway, or local Postgres all work)
DATABASE_URL="postgresql://user:password@localhost:5432/bob"

# Stripe (test mode keys to start — see README "Billing" section for what's still a TODO)
STRIPE_SECRET_KEY="sk_test_..."
STRIPE_PRICE_ID_INDIVIDUAL="price_..."
STRIPE_PRICE_ID_AGENCY="price_..."
STRIPE_WEBHOOK_SECRET="whsec_..."

# Auth — not implemented in this scaffold, see README "Auth" section
# AUTH_SECRET="..."

PORT=4000
